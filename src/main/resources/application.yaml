server:
  port: 8086

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/devprofileproject
    username: postgres
    password: medo1407
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

logging:
  level:
    org.hibernate.SQL: debug

openai:
  api:
    key: ${OPENAI_API_KEY:ollama}
    model: ${OPENAI_MODEL:qwen3:8b}
    base-url: http://localhost:11434/v1 
